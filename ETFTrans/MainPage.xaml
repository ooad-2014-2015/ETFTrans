<phone:PhoneApplicationPage
    x:Class="ETFTrans.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ETFTrans"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True" FontSize="3">



    <phone:PhoneApplicationPage.Resources>
        <vm:testViewModel x:Key="mojModel"/>
        <Style TargetType="Button" x:Key="izgledDugmeta">
            <Setter Property="Background" Value="#FF00B0E4"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF766C6C" Offset="1"/>
                <GradientStop Color="#FF00F3FF" Offset="0.191"/>
                <GradientStop Color="#FF2865D1" Offset="1"/>
                <GradientStop Color="#FF03EEFA" Offset="0.12"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">

            <TextBlock Text="ETFTrans" 
                       Margin="9,-7,0,0" 
                       Style="{StaticResource PhoneTextTitle1Style}" 
                       FontSize="45"
                       Foreground="Black"
                       FontWeight="Black"/>

        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="_long" Margin="0,10,24,0" Grid.Row="1">

            <Button x:Name="odredisteDugme" Content="{Binding Odrediste}"
                    HorizontalAlignment="Left" 
                    Margin="10,65,0,0"
                    VerticalAlignment="Top"
                    Height="96" Width="436"
                    Style="{StaticResource izgledDugmeta}" 
                    Click="Button_Click_1"/>

            <Button 
                    x:Name="polazisteDugme" Content="{Binding Polaziste}" 
                    HorizontalAlignment="Left" 
                    Margin="10,-31,0,0" 
                    VerticalAlignment="Top" 
                    Height="96" Width="436" 
                    Click="Button_Click" 
                    Style="{StaticResource izgledDugmeta}"/>
            <Button x:Name="traziDugme" Content="Trazi"
            	HorizontalAlignment="Left" 
            	Margin="20,161,0,0"
            	VerticalAlignment="Top"
            	Height="96" Width="436"
            	Style="{StaticResource izgledDugmeta}" Click="traziDugme_Click" 
            	/>

            <TextBlock x:Name="tb1" HorizontalAlignment="Left" Height="206" Margin="56,400,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Width="368" FontSize="20"/>
            <Button x:Name="testPretrage" Content="test Pretrage" HorizontalAlignment="Left" Margin="20,283,0,0" VerticalAlignment="Top" Width="384" Click="testPretrage_Click"/>





        </Grid>


        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>